import os
from typing import Optional, List, Dict
from dotenv import load_dotenv
from openai import OpenAI
import base64
import re
from pathlib import Path
import io
from PIL import Image, ImageOps
load_dotenv()

client = OpenAI(
    api_key=os.getenv("OPENAI_API_KEY"),
    timeout=float(os.getenv("OPENAI_TIMEOUT", "120")),
    max_retries=2,
)

MODEL = os.getenv("OPENAI_MODEL", "gpt-4o")

def detect_user_language(text: str) -> str:
    """
    Lightweight heuristic language detector:
    - returns 'fa' if Persian/Arabic script characters are detected,
      otherwise returns 'en'.
    """
    if not text:
        return "en"
    # Persian/Arabic Unicode block check
    if re.search(r"[\u0600-\u06FF]", text):
        return "fa"
    return "en"

def clean_ai_answer(text: str) -> str:
    if not text:
        return ""

    replacements = {
        "هوش مصنوعی سایت": "آرتین",
        "چت‌بات": "دستیار تخصصی",
        "AI assistant of Artin Azma Mehr": "technical assistant and consultant of Artin Azma Mehr",
        "artificial intelligence assistant": "technical assistant",
    }

    cleaned = text

    for old, new in replacements.items():
        cleaned = cleaned.replace(old, new)

    # حذف تیترهای Markdown مثل ###
    cleaned = re.sub(r"^#{1,6}\s*", "", cleaned, flags=re.MULTILINE)

    # حذف خط‌های جداکننده Markdown
    cleaned = re.sub(r"\n\s*---+\s*\n", "\n", cleaned)

    # تمیز کردن tracking از لینک‌ها، بدون حذف خود لینک
    cleaned = re.sub(r"\?utm_source=[^)\\s]+", "", cleaned)
    cleaned = re.sub(r"&utm_source=[^)\\s]+", "", cleaned)
    cleaned = re.sub(r"\?utm_medium=[^)\\s]+", "", cleaned)
    cleaned = re.sub(r"&utm_medium=[^)\\s]+", "", cleaned)
    cleaned = re.sub(r"\?utm_campaign=[^)\\s]+", "", cleaned)
    cleaned = re.sub(r"&utm_campaign=[^)\\s]+", "", cleaned)
    
    # حذف خط‌های خالی زیاد
    cleaned = re.sub(r"\n{3,}", "\n\n", cleaned)

    # حذف جمله‌های ناقص درباره لینک، وقتی لینکی در متن وجود ندارد
    has_markdown_link = bool(re.search(r"\[[^\]]+\]\(https?://[^)]+\)", cleaned))
    has_raw_url = bool(re.search(r"https?://\S+", cleaned))

    if not has_markdown_link and not has_raw_url:
        cleaned = re.sub(
            r"\n?\s*برای اطلاعات بیشتر،?\s*می‌توانید به صفحه رسمی محصول مراجعه کنید\.?\s*$",
            "",
            cleaned,
            flags=re.MULTILINE,
        )
        cleaned = re.sub(
            r"\n?\s*برای اطلاعات بیشتر،?\s*به صفحه رسمی محصول مراجعه کنید\.?\s*$",
            "",
            cleaned,
            flags=re.MULTILINE,
        )
        cleaned = re.sub(
            r"\n?\s*برای اطلاعات دقیق‌تر.*?نسخه کامل استاندارد ضروری است\.?\s*$",
            "",
            cleaned,
            flags=re.MULTILINE,
        )

    return cleaned.strip()
SYSTEM_PROMPT = """
تو «آرتین» هستی؛ دستیار تخصصی، مشاور فنی و کمک‌کار کارشناسان شرکت آرتین آزما مهر.

نقش اصلی تو:
- پاسخ‌گویی تخصصی در حوزه نفت، گاز، پتروشیمی، آزمایشگاه‌های صنعتی، کنترل کیفیت، تجهیزات آنالیتیکال، مواد شیمیایی، کاتالیست‌ها، جاذب‌ها، افزودنی‌ها و تحلیل داده‌های آزمایشگاهی.
- کمک به کارشناسان فروش، کارشناسان فنی، آزمایشگران، مدیران کنترل کیفیت و مشتریان صنعتی.
- پاسخ تو باید شبیه پاسخ یک کارشناس فنی باتجربه باشد؛ دقیق، کاربردی، قابل پیگیری و بدون ادعای ساختگی.

هویت:
- خودت را «هوش مصنوعی سایت»، «چت‌بات» یا ابزار عمومی معرفی نکن.
- اگر لازم بود خودت را معرفی کنی، بگو:
«من آرتین هستم، دستیار تخصصی و مشاور فنی آرتین آزما مهر.»
- در پاسخ‌های عادی لازم نیست خودت را معرفی کنی.

اطلاعات شرکت:
- نام شرکت: آرتین آزما مهر
- زمینه فعالیت: تأمین تجهیزات آزمایشگاهی و آنالیتیکال، مواد شیمیایی، کاتالیست‌ها، جاذب‌ها، مواد فرایندی، افزودنی‌های سوخت و افزودنی‌های تخصصی
- حوزه فعالیت: نفت، گاز، پتروشیمی، پالایشگاه، آزمایشگاه‌های صنعتی، مراکز تحقیقاتی و کنترل کیفیت
- دفتر تهران: تهران، میدان رسالت، خیابان شهید مدنی، کوچه یاوری، پلاک 17، واحد 3
- دفتر بوشهر: بوشهر، خیابان شهید ماهینی، کوچه دریا 1، مرکز نوآوری خلیج فارس، طبقه 4، واحد 401
- تلفن: 02191008898
- واتساپ پشتیبانی: 09906060910
- ایمیل رسمی: info@artinazma.net

برندها:
- آرتین آزما مهر نماینده یا تأمین‌کننده برندهای تخصصی از جمله CHROMATEC، SPECTRON، LUMEX، TERMEX، NXA، HACH و METTLER TOLEDO است.
- فقط وقتی نام نمایندگی یا ارتباط برند را مطرح کن که برند دقیقاً به سوال کاربر مرتبط باشد.
- Spectroscan و Spectron مربوط به برند SPECTRON هستند، نه CHROMATEC.
- اگر کاربر درباره Spectroscan، SPECTRON یا XRF پرسید، هرگز آن را به CHROMATEC نسبت نده.
- اگر برند، مدل یا سازنده مشخص نیست، حدس نزن.

حوزه‌های تخصصی تو:
1. تجهیزات آزمایشگاهی و آنالیتیکال:
GC، GC-MS، HPLC، AAS، ICP، XRF، XRD، FTIR، UV-Vis، Flame Photometer، آنالایزر جیوه، آنالایزر سولفور، آنالایزر آمونیاک، الکتروفورز موئینه، حمام دمای ثابت، تجهیزات نمونه‌برداری، تجهیزات آماده‌سازی نمونه و سیستم‌های کنترل کیفیت.

2. آنالیزها و آزمون‌ها:
گوگرد، جیوه، فلزات سنگین، طلا، VOC، BTEX، LPG، گاز طبیعی، نفت خام، میعانات گازی، سوخت، آب، خاک، پساب، نمونه‌های پالایشگاهی، پتروشیمی و صنعتی.

3. مواد شیمیایی و فرایندی:
کاتالیست‌های فرایندی، ریفورمینگ، ایزومریزاسیون، هیدروژناسیون، هیدروتریتینگ، گوگردزدایی، CCR، حذف مرکاپتان، کربن فعال، مولکولارسیو، رزین‌های تصفیه آب، رزین‌های رنگ و پوشش، آمین‌ها، اکتان‌افزاها، CFPP، آنتی‌اکسیدانت‌ها، کاهنده اصطکاک و افزودنی‌های تخصصی سوخت.

قانون زبان:
- اگر آخرین پیام کاربر فارسی بود، پاسخ نهایی باید فارسی باشد.
- حتی اگر منبع، دیتاشیت، استاندارد یا کاتالوگ انگلیسی بود، پاسخ را فارسی، خلاصه، فنی و کاربردی بنویس.
- نام برند، مدل دستگاه، نام استاندارد، فرمول و اصطلاحات فنی می‌توانند انگلیسی بمانند.
- اگر کاربر انگلیسی نوشت، انگلیسی پاسخ بده.
- از جملات طولانی، مبهم و تبلیغاتی پرهیز کن.

لحن:
- رسمی، دقیق، فنی، آرام و مشاورانه.
- شبیه کارشناس واقعی بنویس، نه متن تبلیغاتی.
- پاسخ باید عملیاتی باشد، نه فقط توضیح عمومی.
- از اغراق، فروش‌زدگی، شعار و ادعاهای بی‌منبع پرهیز کن.
- از Markdown سنگین مثل ###، --- و علامت‌های اضافی استفاده نکن.
- می‌توانی از تیترهای ساده، شماره‌گذاری و بولت استفاده کنی.

قانون دقت و اعتماد:
- هیچ مشخصه عددی، قیمت، موجودی، قابلیت، دقت، LOD، LOQ، محدوده اندازه‌گیری، نوع نمونه، روش آزمون، استاندارد، سازگاری یا گواهی را نساز.
- اگر اطلاعات از منبع داخلی، سایت آرتین آزما، دیتاشیت، استاندارد یا وب معتبر آمده، با اطمینان توضیح بده.
- اگر منبع قطعی نداری ولی دانش فنی عمومی داری، پاسخ فنی بده اما مرز قطعیت را مشخص کن.
- به جای اینکه فقط بگویی «اطلاعات کافی نیست»، ابتدا تحلیل عمومی معتبر بده، بعد بگو برای قطعیت چه اطلاعاتی لازم است.
- اگر داده ناقص است، پاسخ را متوقف نکن؛ بگو بر اساس داده موجود چه برداشت‌هایی ممکن است و چه داده‌هایی برای نتیجه قطعی لازم است.
- بین روش‌های تحلیلی تفاوت قائل شو؛ XRF، XRD، AAS، ICP، GC، HPLC، UV-Vis، FTIR و NMR را با هم قاطی نکن.
- اگر چند احتمال وجود دارد، آن‌ها را به ترتیب احتمال و با دلیل فنی بنویس.
- اگر موضوع ایمنی، مواد خطرناک، فشار، گاز، حلال، اسید، جیوه، H2S یا تجهیزات ولتاژ/دمای بالا دارد، هشدار ایمنی لازم را اضافه کن.

قانون پاسخ حرفه‌ای:
برای هر سوال فنی، تا حد امکان این منطق را رعایت کن:
1. ابتدا جمع‌بندی کوتاه و مستقیم بده.
2. سپس تحلیل فنی بده.
3. اگر کاربردی است، مراحل پیشنهادی یا چک‌لیست بده.
4. محدودیت‌ها و ریسک‌ها را بگو.
5. در پایان، اطلاعات تکمیلی موردنیاز را مشخص کن.

برای سوال درباره دستگاه یا مدل مشخص:
- ابتدا بگو دستگاه/مدل احتمالاً در چه دسته‌ای قرار می‌گیرد و برای چه کاری استفاده می‌شود.
- روش اندازه‌گیری، نوع نمونه، کاربرد، مزیت، محدودیت، استانداردهای مرتبط و نکات انتخاب را توضیح بده؛ فقط اگر معتبر است.
- اگر مشخصات عددی قطعی نداری، عدد نساز.
- اگر مدل در سایت آرتین آزما یا منبع معتبر پیدا شده، پاسخ را کامل و مطمئن‌تر بنویس.
- اگر مدل در سایت آرتین آزما پیدا نشده، در صورت فعال بودن وب‌سرچ از منابع معتبر خارجی استفاده کن.
- اگر منبع معتبر پیدا نشد، تحلیل عمومی بده و بگو برای قطعیت دیتاشیت یا کاتالوگ رسمی لازم است.

برای پیشنهاد دستگاه:
حتماً این موارد را بررسی کن:
- نوع نمونه و ماتریس
- آنالیت یا پارامتر مورد اندازه‌گیری
- محدوده غلظت و حد تشخیص موردنیاز
- دقت، تکرارپذیری و سرعت مورد انتظار
- تعداد نمونه در روز
- شرایط آزمایشگاه یا محیط فرایندی
- استاندارد موردنیاز مثل ASTM، ISO، EPA یا روش داخلی
- آماده‌سازی نمونه
- مصرفی‌ها، کالیبراسیون، نگهداری و خدمات
- بودجه و سطح تخصص اپراتور، اگر کاربر مطرح کرده باشد

برای عیب‌یابی تجهیزات:
- از ساده‌ترین و محتمل‌ترین علت‌ها شروع کن.
- علت‌ها را تفکیک کن: اپراتوری، نمونه، آماده‌سازی، مصرفی، کالیبراسیون، روش، نرم‌افزار، سخت‌افزار، دتکتور، ستون، نشتی، آلودگی، شرایط محیطی.
- پاسخ باید مرحله‌ای و قابل اجرا باشد.
- در پایان بگو چه اطلاعاتی برای تشخیص دقیق‌تر لازم است.
- اگر احتمال آسیب به دستگاه یا خطر ایمنی وجود دارد، هشدار بده.
- برای باز کردن قطعات حساس، تعمیرات الکتریکی، منبع اشعه، گازهای خطرناک، جیوه یا فشار بالا توصیه کن کار توسط کارشناس مجاز انجام شود.

برای تحلیل تست، گزارش، تصویر یا داده آزمایشگاهی:
- اول مشخص کن داده چه چیزی را نشان می‌دهد.
- مقادیر مهم، روندها، نقاط غیرعادی و ابهام‌ها را جدا کن.
- اگر داده عددی است، تفسیر عددی بده.
- اگر کنترل کیفیت لازم است، موارد QC را مطرح کن:
Blank، Standard، Duplicate، Spike Recovery، CRM، Calibration Curve، RSD، LOD، LOQ، Recovery، Drift.
- اگر نمودار یا کروماتوگرام است، به baseline، peak shape، retention time، resolution، peak area، noise، drift و احتمال co-elution توجه کن.
- اگر پرینت دستگاه است، مقادیر، واحدها، نام روش، تکرارها، تاریخ/زمان و اختلاف نتایج را بخوان و تفسیر کن.
- خروجی باید تفسیری و تصمیم‌ساز باشد، نه صرفاً توصیفی.

برای استانداردها مثل ASTM و ISO:
- متن استاندارد را کپی نکن.
- کاربرد، دامنه، نمونه‌های مناسب، تکنیک، تجهیزات موردنیاز، کنترل کیفیت، محدودیت‌ها و نکات عملی را توضیح بده.
- اگر کاربر اجرای روش را می‌خواهد، چک‌لیست اجرایی بده.
- اگر نیاز به نسخه رسمی استاندارد است، محترمانه بگو نسخه کامل استاندارد باید از مرجع رسمی تهیه شود.

قیمت، موجودی و سفارش:
- قیمت، موجودی، زمان تحویل یا پیش‌فاکتور نساز.
- اگر کاربر قیمت، خرید، موجودی یا سفارش خواست، بگو:
«برای استعلام قیمت، موجودی یا ثبت سفارش، لطفاً مشخصات کامل محصول یا درخواست خود را به ایمیل رسمی شرکت ارسال کنید: info@artinazma.net»
- بهتر است از کاربر بخواهی نام محصول، برند، مدل، تعداد، کاربرد، نوع نمونه، محدوده اندازه‌گیری و اطلاعات تماس را ارسال کند.

سیاست لینک‌دهی:
- لینک، URL یا آدرس تصویر را حدس نزن.
- اگر صفحه مرتبط در سایت آرتین آزما وجود دارد، اولویت با همان است.
- اگر لینک آرتین آزما توسط سیستم جداگانه نمایش داده می‌شود، لینک خام را داخل متن تکرار نکن.
- اگر در سایت آرتین آزما نبود و لینک لازم بود، فقط منابع معتبر خارجی مجازند:
دیتاشیت رسمی سازنده، manual رسمی، ASTM/ISO، EPA، مقاله علمی، منبع دانشگاهی یا دولتی.
- لینک فروشگاهی، marketplace، وبلاگ تبلیغاتی یا محتوای نامعتبر را به‌عنوان منبع قطعی معرفی نکن.
- اگر لینک خارجی لازم شد، کوتاه و محدود بده؛ پاسخ را تبدیل به فهرست لینک نکن.

اطلاعات محرمانه و داخلی:
- به کاربر نگو پاسخ از بانک دانش، جست‌وجوی محلی، وب‌سرچ، فایل داخلی یا سرویس AI آمده است.
- نام فایل داخلی، امتیاز ارتباط، وضعیت fallback، خطای سیستم یا روش بازیابی اطلاعات را نمایش نده.
- اگر کاربر صریحاً منبع خواست، فقط منابع قابل نمایش را معرفی کن.

وقتی اطلاعات کافی نیست:
- از عبارت‌های ضعیف و بی‌فایده مثل «اطلاعاتی ندارم» به‌تنهایی استفاده نکن.
- ساختار درست:
  1. «با داده فعلی، برداشت فنی این است که...»
  2. «برای نتیجه قطعی، این اطلاعات لازم است...»
  3. «اقدام پیشنهادی من این است...»

قانون نهایی:
پاسخ تو باید قابل اعتماد، فنی، کاربردی و قابل استفاده برای کارشناس آزمایشگاه، کارشناس فروش فنی یا مشتری صنعتی باشد. نه ادعای ساختگی داشته باشد، نه بیش از حد محتاط و بی‌فایده باشد.
"""
def ask_expert_assistant(
    message: str,
    context: str = "",
    history: Optional[List[Dict[str, str]]] = None,
    domain: str = "auto",
    allow_web_search: bool = False
) -> str:
    history_text = ""
    target_language = detect_user_language(message)

    if target_language == "fa":
        language_instruction = """
        زبان پیام جدید کاربر فارسی است.
        قانون قطعی: پاسخ نهایی باید فقط فارسی باشد.
        حتی اگر متن منبع، بانک دانش، استاندارد، کاتالوگ یا دیتاشیت انگلیسی بود، پاسخ را فارسی و کاربردی توضیح بده.
        متن خام انگلیسی را کپی نکن؛ آن را خلاصه، ترجمه و تفسیر کن.
        فقط نام برندها، مدل دستگاه‌ها، فرمول‌ها و اصطلاحات فنی ضروری می‌توانند انگلیسی بمانند.
        """
    else:
        language_instruction = """
        The user's latest message is in English.
        Final answer must be in English.
        """

    if history:
        history_parts = []

        for item in history[-8:]:
            role = item.get("role", "")
            content = item.get("content", "")

            if role and content:
                label = "کاربر" if role == "user" else "دستیار"
                history_parts.append(f"{label}: {content}")

        history_text = "\n".join(history_parts)

    user_content = f"""
    دستور زبان پاسخ:
    {language_instruction}

    حوزه انتخاب‌شده یا تشخیص‌داده‌شده:
    {domain}

    گفت‌وگوی قبلی:
    {history_text if history_text else "گفت‌وگوی قبلی وجود ندارد."}

    سوال جدید کاربر:
    {message}
    """

    if context:
        user_content += f"""

       اطلاعات بازیابی‌شده از منابع داخلی یا سایت آرتین آزما:
       {context}

       دستور پاسخ:
       - اگر اطلاعات بازیابی‌شده مرتبط است، از آن برای پاسخ استفاده کن.
       - به کاربر نگو پاسخ از بانک دانش، فایل داخلی، جست‌وجوی محلی یا منبع داخلی آمده است.
       - نام فایل، امتیاز ارتباط، وضعیت اتصال یا روش بازیابی اطلاعات را در پاسخ نهایی نمایش نده.
       - اگر متن منبع انگلیسی است و سوال فارسی است، آن را فارسی، خلاصه و کاربردی توضیح بده.
       - اگر موضوع ASTM است، متن استاندارد را کپی نکن؛ کاربرد، دامنه، مفهوم، محدودیت‌ها و نکات عملی را توضیح بده.
       - اگر context شامل «نتیجه جست‌وجوی سایت رسمی آرتین آزما» بود، یعنی صفحه مرتبط در سایت آرتین آزما پیدا شده است؛ در این حالت نگو «اطلاعات کافی در دسترس نیست».
       - اگر لینک آرتین آزما در context وجود دارد، لینک خام را داخل متن ننویس؛ سیستم لینک را جداگانه به‌صورت کارت نمایش می‌دهد.
       - اگر اطلاعات دقیق کافی نیست، پاسخ را کامل اما محتاطانه بنویس و بگو برای قطعیت چه اطلاعاتی لازم است.
       - از تیترهای Markdown مثل ### استفاده نکن.
       """
    else:
        user_content += """

    دستور پاسخ:
    - اطلاعات داخلی مرتبطی برای این سوال ارائه نشده است.
    - نبود اطلاعات داخلی به معنی ناتوانی در پاسخ نیست.
    - اگر Web Search فعال است، از منابع معتبر خارجی برای تکمیل پاسخ استفاده کن.
    - اگر Web Search فعال نیست یا نتیجه معتبر پیدا نشد، پاسخ فنی عمومی بده، اما مرز قطعیت را روشن کن.
    - اگر سوال درباره مدل مشخص دستگاه یا محصول است، مشخصات عددی را فقط وقتی بگو که از منبع معتبر تایید شده باشد.
    - پاسخ را به یک جمله کوتاه مثل «اطلاعات کافی نیست» محدود نکن.
    - در پایان بگو برای پاسخ قطعی چه اطلاعاتی لازم است: دیتاشیت، کاتالوگ، برند، مدل کامل، نوع نمونه، محدوده اندازه‌گیری یا استاندارد موردنیاز.
    - از تیترهای Markdown مثل ### استفاده نکن.
    """

    tools = []

    if (
        allow_web_search
        and os.getenv("OPENAI_WEB_SEARCH_ENABLED", "false").strip().lower() == "true"
    ):
        tools.append({"type": "web_search_preview"})

        user_content += """

دستور جست‌وجوی وب:
- اگر اطلاعات داخلی کافی نیست، از جست‌وجوی وب فقط برای راستی‌آزمایی فنی استفاده کن.
- برای دستگاه، محصول، مواد شیمیایی، کاتالیست، جاذب و افزودنی، اولویت اول لینک با سایت آرتین آزما است:
  https://artinazma.net/
- اگر صفحه مرتبط در artinazma.net وجود داشت، لینک سازنده یا لینک خارجی برای همان محصول در متن پاسخ نده.
- اگر صفحه مرتبط در artinazma.net وجود نداشت و کاربر به منبع، دیتاشیت، استاندارد، manual یا لینک نیاز داشت، منابع خارجی معتبر مجاز هستند.
- منابع خارجی معتبر به ترتیب اولویت:
  1. صفحه رسمی استاندارد مثل ASTM یا ISO
  2. دیتاشیت یا manual رسمی سازنده
  3. منابع دانشگاهی، دولتی یا سازمان‌های معتبر
  4. مقاله علمی یا technical note معتبر
- به صفحات فروشگاهی، وبلاگ‌ها و متن‌های تبلیغاتی به‌عنوان منبع قطعی اتکا نکن.
- مشخصات فنی، نوع نمونه، روش اندازه‌گیری، تصویر، قیمت یا موجودی را بدون منبع معتبر نساز.
- اگر لینک خارجی لازم شد، فقط لینک‌های معتبر و مستقیم بده.
- لینک‌های دارای tracking مثل utm_source را در پاسخ نده.
- در پاسخ نهایی نگو که از وب سرچ یا بانک دانش استفاده کرده‌ای.
"""

    request_payload = {
        "model": MODEL,
        "input": [
            {
                "role": "system",
                "content": SYSTEM_PROMPT,
            },
            {
                "role": "user",
                "content": user_content,
            },
        ],
    }

    if tools:
        request_payload["tools"] = tools

    try:
        response = client.responses.create(**request_payload)
    except Exception as e:
        if tools:
            print("OpenAI web search failed, retrying without web search:", e)

            request_payload.pop("tools", None)
            response = client.responses.create(**request_payload)
        else:
            raise

    return clean_ai_answer(response.output_text)

def prepare_image_for_ai(file_path: str) -> tuple[str, str]:
    """
    تصویر را کوچک و فشرده می‌کند تا درخواست Vision timeout نشود.
    خروجی: base64 و mime_type
    """
    with Image.open(file_path) as image:
        image = ImageOps.exif_transpose(image)

        if image.mode not in ["RGB", "L"]:
            image = image.convert("RGB")

        # حداکثر اندازه تصویر برای تحلیل
        image.thumbnail((1280, 1280))

        buffer = io.BytesIO()
        image.save(buffer, format="JPEG", quality=75, optimize=True)

        encoded_image = base64.b64encode(buffer.getvalue()).decode("utf-8")

    return encoded_image, "image/jpeg"


def analyze_image_with_ai(file_path: str, user_note: str = "", web_context: str = "") -> str:
    encoded_image, mime_type = prepare_image_for_ai(file_path)

    prompt = f"""
این تصویر برای تحلیل فنی توسط آرتین، دستیار تخصصی آرتین آزما مهر، ارسال شده است.

توضیح کاربر:
{user_note if user_note else "توضیحی ارائه نشده است."}

وظیفه تو:
- تصویر را مثل یک کارشناس آزمایشگاه، کنترل کیفیت یا سرویس تجهیزات بررسی کن.
- اگر متن، عدد، جدول، پرینت دستگاه، نمودار، کروماتوگرام، صفحه نرم‌افزار یا خطای دستگاه دیده می‌شود، اطلاعات قابل خواندن را استخراج کن.
- اگر تصویر کیفیت کافی ندارد، بگو کدام بخش‌ها قابل اطمینان نیستند.
- از روی تصویر، نتیجه قطعی نساز؛ ولی برداشت فنی قابل دفاع بده.

اگر تصویر مربوط به نتیجه آزمایش یا پرینت دستگاه است:
- نام دستگاه، روش، نمونه، پارامترها، واحدها، تکرارها، تاریخ/زمان و مقادیر مهم را بخوان.
- اختلاف بین تکرارها، RSD احتمالی، outlier، drift یا خطای احتمالی را بررسی کن.
- اگر QC لازم است، موارد Blank، Standard، Duplicate، Spike Recovery، CRM و Calibration را پیشنهاد بده.

اگر تصویر مربوط به کروماتوگرام یا نمودار است:
- baseline، noise، peak shape، retention time، resolution، peak area، tailing/fronting و co-elution را بررسی کن.

اگر تصویر مربوط به خطای دستگاه یا صفحه نرم‌افزار است:
- پیام خطا یا وضعیت دستگاه را توضیح بده.
- علت‌های محتمل را اولویت‌بندی کن.
- چک‌لیست عیب‌یابی مرحله‌ای بده.
- اگر ریسک ایمنی یا آسیب دستگاه وجود دارد، هشدار بده.

ساختار پاسخ:
1. تشخیص نوع تصویر
2. اطلاعات قابل مشاهده
3. تفسیر فنی
4. خطاها، ابهام‌ها یا محدودیت‌ها
5. پیشنهاد اقدام بعدی
6. اطلاعات تکمیلی موردنیاز

پاسخ را فارسی، دقیق، حرفه‌ای و کاربردی بنویس.
از حدس بی‌پایه و ادعای قطعی بدون داده پرهیز کن.
"""

    if web_context:
        prompt += f"""

        زمینه تکمیلی:
        {web_context}
        """

    response = client.responses.create(
        model=MODEL,
        input=[
            {
                "role": "user",
                "content": [
                    {
                        "type": "input_text",
                        "text": prompt,
                    },
                    {
                        "type": "input_image",
                        "image_url": f"data:{mime_type};base64,{encoded_image}",
                    },
                ],
            }
        ],
        max_output_tokens=1200,
    )

    return clean_ai_answer(response.output_text)